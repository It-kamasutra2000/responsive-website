const initialState = {
    
};

export type InitialStateType = typeof initialState


const CheckoutPageReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state
    }

}



export default CheckoutPageReducer;